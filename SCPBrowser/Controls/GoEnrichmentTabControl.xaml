<UserControl x:Class="SCPBrowser.GoEnrichmentTabControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="900" Background="WhiteSmoke">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                BorderBrush="#e2e8f0" 
                BorderThickness="1" 
                CornerRadius="3"
                Padding="8,5" 
                Margin="0,0,0,5" 
                Background="#f8fafc">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="SummaryText"
                           Grid.Column="0"
                           Text="Select points to view GO enrichment analysis"
                           TextTrimming="CharacterEllipsis"
                           FontSize="11"
                           Foreground="#334155"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"/>

                <TextBlock x:Name="StatisticsText"
                           Grid.Column="1"
                           Text="0 GO terms displayed"
                           FontSize="10"
                           Foreground="#64748b"
                           VerticalAlignment="Center"
                           Margin="15,0,0,0"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    Margin="2,0,0,5">
            <TextBlock Text="Filter by GO Namespace:" 
                       VerticalAlignment="Center"
                       FontSize="11"
                       FontWeight="SemiBold"
                       Foreground="#334155"
                       Margin="0,0,10,0"/>

            <RadioButton x:Name="AllNamespacesRadio"
                         Content="All"
                         IsChecked="True"
                         GroupName="Namespace"
                         Margin="0,0,15,0"
                         FontSize="11"
                         VerticalAlignment="Center"
                         Checked="NamespaceFilter_Changed"/>

            <RadioButton x:Name="BiologicalProcessRadio"
                         Content="Biological Process"
                         GroupName="Namespace"
                         Margin="0,0,15,0"
                         FontSize="11"
                         VerticalAlignment="Center"
                         Checked="NamespaceFilter_Changed"/>

            <RadioButton x:Name="MolecularFunctionRadio"
                         Content="Molecular Function"
                         GroupName="Namespace"
                         Margin="0,0,15,0"
                         FontSize="11"
                         VerticalAlignment="Center"
                         Checked="NamespaceFilter_Changed"/>

            <RadioButton x:Name="CellularComponentRadio"
                         Content="Cellular Component"
                         GroupName="Namespace"
                         Margin="0,0,15,0"
                         FontSize="11"
                         VerticalAlignment="Center"
                         Checked="NamespaceFilter_Changed"/>
        </StackPanel>

        <DataGrid x:Name="GoTermsGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  GridLinesVisibility="All"
                  HeadersVisibility="All"
                  SelectionMode="Extended"
                  AlternatingRowBackground="#f9fafb"
                  RowBackground="White"
                  BorderThickness="1"
                  BorderBrush="#e2e8f0">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#e2e8f0"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="8,5"/>
                    <Setter Property="BorderBrush" Value="#cbd5e1"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="Foreground" Value="#334155"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Run Name" Binding="{Binding RunName}" Width="*"/>
                <DataGridTextColumn Header="GO Term" Binding="{Binding GoTermName}" Width="2*"/>
                <DataGridTextColumn Header="GO ID" Binding="{Binding GoTermId}" Width="100"/>
                <DataGridTextColumn Header="Namespace" Binding="{Binding Namespace}" Width="150"/>
                <DataGridTextColumn Header="P-Value" Binding="{Binding PValue, StringFormat=E2}" Width="100"/>
                <DataGridTextColumn Header="Fold Enrichment" Binding="{Binding FoldEnrichment, StringFormat=F2}" Width="120"/>
                <DataGridTextColumn Header="Overlap" Binding="{Binding OverlapText}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>