<UserControl x:Class="SCPBrowser.Controls.MarkerClassesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900"
             Background="White">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Marker Classes" FontSize="16" FontWeight="Bold" Foreground="#0f172a"/>
            <TextBlock Text="Define cell classes by marker genes (no expression profile needed) and classify cells on markers alone."
                       FontSize="11" Foreground="#64748b" Margin="0,2,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0" Margin="0,0,12,0">
                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,6,0,0">
                    <Button Content="+ Add" Click="AddClass_Click" Padding="10,4"/>
                    <Button Content="Delete" Click="DeleteClass_Click" Padding="10,4" Margin="6,0,0,0"/>
                </StackPanel>
                <ListBox x:Name="ClassList" SelectionChanged="ClassList_SelectionChanged"
                         BorderBrush="#e2e8f0" BorderThickness="1"/>
            </DockPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Text="Class name" FontWeight="SemiBold" Foreground="#334155"/>
                <TextBox x:Name="NameBox" Padding="6" Margin="0,2,0,10"/>
                <TextBlock Text="Marker genes (one per line, or comma / space separated)" FontWeight="SemiBold" Foreground="#334155"/>
                <TextBox x:Name="MarkersBox" AcceptsReturn="True" TextWrapping="Wrap" Height="220"
                         VerticalScrollBarVisibility="Auto" Padding="6" Margin="0,2,0,10" FontFamily="Consolas"/>
                <Button Content="Save class" Click="SaveClass_Click" Padding="14,5" HorizontalAlignment="Left"
                        Background="#2563eb" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Background="#f8fafc" BorderBrush="#e2e8f0" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,10,0,0">
            <StackPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Min markers to assign:" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,6,0"/>
                    <xctk:IntegerUpDown x:Name="MinMarkersBox" Minimum="1" Maximum="50" Value="2" Width="70" ValueChanged="MinMarkers_ValueChanged"/>
                    <Button x:Name="ClassifyButton" Content="Classify cells" Click="Classify_Click" Padding="16,5" Margin="16,0,0,0"
                            Background="#10b981" Foreground="White" BorderThickness="0" FontWeight="SemiBold" Cursor="Hand"/>
                    <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="14,0,0,0" Foreground="#64748b"/>
                </StackPanel>
                <TextBlock x:Name="SummaryText" Margin="0,8,0,0" TextWrapping="Wrap" Foreground="#334155"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
