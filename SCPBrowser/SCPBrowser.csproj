<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0-windows7.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <ApplicationIcon>images\SCPBrowser.ico</ApplicationIcon>
  </PropertyGroup>

  <ItemGroup>
    <None Remove="images\logo.png" />  
    <None Remove="images\Logo2.png" />  
    <None Remove="images\PLV.png" /></ItemGroup>

  <ItemGroup>
    <Content Include="images\logo.png">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="images\PLV.png">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Content Include="images\SCPBrowser.ico" />
    <!-- Built-in user guide, opened from Help ▸ User Guide (F1). Copied next to the executable so it ships
         with the build and with ClickOnce deployments. -->
    <Content Include="Help\SCPBrowserHelp.html">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Content>
    <Resource Include="images\logo2.png">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </Resource>
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Extended.Wpf.Toolkit" Version="5.1.2" />
    <PackageReference Include="MathNet.Numerics" Version="5.0.0" />
    <PackageReference Include="Microsoft.Data.Sqlite.Core" Version="10.0.10" />
    <!-- Pinned to 5.x deliberately. Parquet.Net 6.0 is a breaking change for this code: ParquetReader no longer
         implements IDisposable and the row-group column reads moved, so ParquetDataService does not compile
         against it. Upgrading needs that reader ported and a full re-import test, because every downstream number
         comes through this path. -->
    <PackageReference Include="Parquet.Net" Version="5.4.0" />
    <PackageReference Include="ScottPlot.WPF" Version="5.1.59" />
    <PackageReference Include="SQLitePCLRaw.bundle_e_sqlite3" Version="3.0.5" />
    <PackageReference Include="UMAP" Version="1.0.62830" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Voronoi\ProteoMappings\BioTessera.csproj" />
    <ProjectReference Include="..\..\Voronoi\Revigo\RevigoCore.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Compile Update="Settings.Designer.cs">
      <DesignTimeSharedInput>True</DesignTimeSharedInput>
      <AutoGen>True</AutoGen>
      <DependentUpon>Settings.settings</DependentUpon>
    </Compile>
  </ItemGroup>

  <ItemGroup>
    <None Update="Settings.settings">
      <Generator>SettingsSingleFileGenerator</Generator>
      <LastGenOutput>Settings.Designer.cs</LastGenOutput>
    </None>
  </ItemGroup>

  <ItemGroup>
    <Folder Include="DLLs\" />
  </ItemGroup>

  <ItemGroup>
    <Reference Include="PatternTools">
      <HintPath>DLLs\PatternTools.dll</HintPath>
    </Reference>
  </ItemGroup>

</Project>
